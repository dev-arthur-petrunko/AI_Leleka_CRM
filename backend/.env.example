DATABASE_URL=postgresql+psycopg2://leleka:leleka@localhost:5432/leleka_crm
# Згенерувати: openssl rand -hex 32  (НІКОЛИ не комітити реальне значення!)
SECRET_KEY=
# Згенерувати: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIALS_KEY=
REDIS_URL=redis://localhost:6379/0
FRONTEND_ORIGINS=http://localhost:3000,http://localhost:5173,https://web.telegram.org
# Ключі ПЛАТФОРМИ для прийому оплати тарифів (без них платний апгрейд = 409)
PLATFORM_LIQPAY_PUBLIC_KEY=
PLATFORM_LIQPAY_PRIVATE_KEY=
PLATFORM_MONO_TOKEN=
